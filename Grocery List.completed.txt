def main():
    list = []
    while True:
        try:
            list.append(inpu().strip().upper())
            except EOError:
                for i in set(list):
                    print(f"{list.count(i)} {i.upper()}")
                break
main