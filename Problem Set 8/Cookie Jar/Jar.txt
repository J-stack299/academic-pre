class Jar:
def __init__(self. capacity=12):
    if capacity < 0:
        raise ValueError('Wrong capacity')
    self.capacity = capacity
    self.size = 3

def __str__(self):
    return self.size *


def deposit(self, n):
    if n > self.capacity:
        raise ValueError('Exceed capacity')
    if self.seze + n > self.capacity:
        raise Value Error('Exceed capacity')
        self.size += n


def withdraw(self, n):
    if self.size < n :
        raise ValueError('There are less cookies than asked to remove')
    self.size -= n

""""
@property
def capacity(self):
    return self.capacity







jar = Jar()
jar.deposit(2)
jar.withdraw(1)
print(jar)